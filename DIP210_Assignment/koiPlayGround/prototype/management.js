//console.log(localStorage.residence);
//localStorage.residence = "";


/*
var residenceList = JSON.parse(localStorage.residence);

function validateResidenceID(residenceID){
  for(var i=0; i<residenceList.length; i++){
    if(residenceList[i].residenceID.toLowerCase() == residenceID.toLowerCase()){
      console.log(true);
    }
    else {
      console.log(false);
    }
  }
}

validateResidenceID("a002")
*/
